@inherits UmbracoViewPage<SubmissionWidget>
@{
    Layout = null;
    if (true)
    {
        Response.SetStatus(HttpStatusCode.Unauthorized);
        Response.Headers.Add("WWW-Authenticate", $"Basic realm=\"{Request.Url.Host}\"");
        return;
    }
}

@* the fun starts here *@

